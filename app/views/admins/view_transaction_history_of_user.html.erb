<div class="center jumbotron">
<h2>Transactions history</h2>
<br>
<%= @user.name %>:<br>
<% if !@accounts.empty? %>
	<% @accounts.each do |account| %> 
	<%= "Account number: #{account.account_id}" %><br>
	  <h4>
		<% account.transactions.each do |transaction| %>
		<% if transaction.status != 4 %>
			<div>
				<%= "Transaction ID: #{transaction.id}" %><br>
				<%= "Transaction status: #{status_name transaction.status}" %><br>
				<%= "Start time: #{transaction.start}" %><br>
				<%= "Finish time: #{transaction.finish}" %><br>
				<%= "#{transaction_name transaction.transaction_type}" %><br>
				<%= "Amount: #{transaction.amount}" %><br>
				<%= "From account number: #{transaction.account.account_id}" %><br>
				<% if transaction.transaction_type == 3 %><br>
					<%= "transfer to account number: #{transaction.transfer.account.account_id}" %>
				<% end %>
				<% if transaction.status == 3 && transaction.transaction_type == 1 %>
					<%= link_to "approve transaction", approve_or_decline_transaction_path(:transaction_id => t.id, :decision => '1', :url => 'history') %>
					<%= link_to "decline transaction", approve_or_decline_transaction_path(:transaction_id => t.id, :decision => '2', :url => 'history') %>
				<% end %>
			</div>
			<br><br>
		<% end %>
		<% end %>
	  </h4>
	<br>
	<% end %>
<% else %>
	<h2>No transactions</h2>
<% end %>

<%= link_to "back to users page", view_users_path() %>
</div>


