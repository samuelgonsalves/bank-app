<h1>Withdrawals</h1>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_tag("", method:"post") do %>

      <%= select_tag "account_id", options_from_collection_for_select(@accounts, "id", "account_id") %>
	  <%= text_field_tag 'amount'%>
	<%= submit_tag "Withdraw" ,:name => nil%>

    <% end %>
  </div>